# Python Dependencies for vansh-local-ai-stack

# Core
requests>=2.28.0
python-dotenv>=1.0.0

# System monitoring
psutil>=5.9.0

# Document processing (for RAG - Phase 2)
pypdf2>=3.0.0
python-docx>=0.21.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# RAG (Phase 2)
chromadb>=0.4.0
langchain>=0.1.0
langchain-community>=0.0.10

# Vector database alternatives (Phase 2)
# qdrant-client>=1.7.0

# Notifications
# Desktop notifications use system tools (notify-send on Linux, osascript on macOS, PowerShell on Windows)
# For email notifications, configure SMTP in .env

# n8n integration (Phase 4)
# n8n itself is installed via npm, but you may want webhook libraries
# webhooks (built into requests)

# Voice input (Phase 5)
# openai-whisper>=20230314  # OpenAI Whisper for voice transcription
# faster-whisper>=0.10.0     # Faster Whisper alternative
